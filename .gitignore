/target/
/classes/
/checkouts/
# scripts/coverage.sh writes its instrumented run + raw cloverage output here.
/testbench/
# Probe and bench transcripts written by a run against a local rig.
/logs/
profiles.clj
# tools/vaelii-top is a Python package in this tree; its virtualenv and the
# editable-install metadata `pip install -e` leaves are per machine.
/tools/*/.venv/
*.egg-info/
__pycache__/
pom.xml
pom.xml.asc
*.jar
*.class
/.lein-*
/.nrepl-port
/.prepl-port
.hgignore
.hg/
.cpcache/
.calva/
.clj-kondo/.cache/
.clj-kondo/imports/
.lsp/.cache/
# AI-helper files: stow-linked from the settings dotfiles repo, never tracked here.
CLAUDE.local.md
/CLAUDE.md
.claude/

# macOS Finder metadata; this is a macOS-authored repo and one of these
# committed once is committed forever.
.DS_Store

# the local downstream roster check-breaking-siblings.sh reads (never published)
.sibling-repos

# handoff notes between concurrent sessions; never tracked
tmp/
