"""vaelii-top — btop's frame around a vaelii checkout.

The status of the repository above, and below it the git history with one
column per check: which revision lint, the suite, perf and the two matrix
selectors last ran at, and what each of them said.
"""

__version__ = "0.1.0"
